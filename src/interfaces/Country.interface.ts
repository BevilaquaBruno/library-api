/**
 * Interfaces for country
 */

/**
 * @CountryData
 * Interface for country pattern data
 */
export interface CountryData {
  id: number;
  name: string;
  fullName: string;
  short: string;
  flag: string;
}
